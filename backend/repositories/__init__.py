"""Repository layer -- see complaint_repository.py's module docstring for the layering this
follows (LangGraph orchestration -> business service -> repository -> database, never LangGraph
-> LLM -> generated SQL).
"""
